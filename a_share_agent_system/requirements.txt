# ============================================================
#  A股多智能体舆情与量化策略系统 — 依赖清单
# ============================================================
#  安装: pip install -r requirements.txt
#  硬件: RTX 5070 Ti (16GB VRAM) / 64GB RAM
# ============================================================

# -------- Phase 1: 数据基础 (当前启用) --------
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # Parquet 读写引擎
baostock>=0.8.8           # A股行情数据源
akshare>=1.12.0           # 港股+新闻数据源
aiohttp>=3.9.0            # 异步HTTP客户端
pydantic>=2.0.0           # 数据校验
duckdb>=0.9.0             # 高性能分析型数据库(内存模式)
chromadb>=0.4.0           # 向量数据库(HNSW索引)

# -------- 工具库 --------
pyyaml>=6.0               # YAML配置文件解析
tqdm>=4.65.0              # 进度条
simhash>=2.1.0            # 新闻去重(SimHash)

# -------- Phase 2: 模型训练 (后续启用) --------
# torch>=2.1.0
# transformers>=4.36.0
# peft>=0.7.0
# accelerate>=0.25.0
# bitsandbytes>=0.41.0
# wandb>=0.16.0

# -------- Phase 3: Agent集成 (后续启用) --------
# langchain>=0.1.0
# llama-index>=0.9.0
# vllm>=0.2.0      # 或 ollama
# streamlit>=1.30.0

# -------- 开发工具 --------
pytest>=7.4.0
ipython>=8.0.0
